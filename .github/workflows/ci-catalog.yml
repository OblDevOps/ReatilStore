name: CI/CD - Catalog

on:
  push:
    branches: [main]
    paths:
      - "src/catalog/**"
      - ".github/workflows/ci-catalog.yml"
  pull_request:
    branches: [main]
    paths:
      - "src/catalog/**"
  workflow_dispatch:

env:
  AWS_REGION: us-east-1

jobs:
  # Detector de secretos con TruffleHog
  secrets-scan:
    name: Secrets Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - name: Detect secrets (TruffleHog)
        uses: trufflesecurity/trufflehog@9b6b5326bfe25dbd856eccc8a8275eb5dea7bd52 # main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --only-verified

  # Análisis estático de código con Semgrep
  sast:
    name: SAST (Semgrep)
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep:v1.167.0
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Run Semgrep
        run: |
          semgrep scan \
            --config=auto \
            --sarif \
            --output=semgrep-results.sarif \
            src/catalog
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}

      - name: Upload Semgrep SARIF
        uses: github/codeql-action/upload-sarif@b0c4fd77f6c559021d78430ec4d0d169ae74a4eb # v3
        if: always()
        with:
          sarif_file: semgrep-results.sarif

  # Análisis de vulnerabilidades de dependencias con Trivy (filesystem)
  sca:
    name: SCA (Trivy filesystem)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Run Trivy (filesystem — dependencies)
        uses: aquasecurity/trivy-action@cf5c088a69634cd13ccddc735d7926162c31f9a6 # master
        with:
          scan-type: fs
          scan-ref: src/catalog
          severity: CRITICAL,HIGH
          exit-code: '1'
          ignore-unfixed: true

  # Pruebas de integración con Newman
  tests:
    name: Integration Tests (Newman)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: src/catalog/go.mod
          cache-dependency-path: src/catalog/go.sum

      - name: Build catalog binary
        working-directory: src/catalog
        env:
          CGO_ENABLED: 1
        run: go build -o catalog .

      - name: Start catalog service
        working-directory: src/catalog
        run: ./catalog &
        env:
          PORT: 8080
          RETAIL_CATALOG_PERSISTENCE_PROVIDER: in-memory

      - name: Wait for catalog to be ready
        run: |
          for i in $(seq 1 15); do
            if curl -sf http://localhost:8080/health; then
              echo "Catalog is up"
              exit 0
            fi
            echo "Waiting... ($i/15)"
            sleep 2
          done
          echo "Catalog never became ready"
          exit 1

      - name: Install Newman
        run: npm install -g newman newman-reporter-htmlextra

      - name: Run Newman tests
        run: |
          newman run tests/postman/catalog.postman_collection.json \
            --reporters cli,htmlextra \
            --reporter-htmlextra-export tests/postman/reports/catalog-report.html \
            --bail

      - name: Upload Newman report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: newman-report-catalog
          path: tests/postman/reports/catalog-report.html

  # Construcción, análisis de imagen y push a ECR
  build-and-push:
    name: Build & Push
    runs-on: ubuntu-latest
    needs: [secrets-scan, sast, sca, tests]
    if: github.event_name != 'pull_request'

    outputs:
      image_uri: ${{ steps.push.outputs.image_uri }}

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ff717079ee2060e4bcee96c4779b553acc87447c # v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64 # v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Build Docker image
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          docker build \
            --label "git.sha=${{ github.sha }}" \
            --label "git.branch=${{ github.ref_name }}" \
            --label "build.date=$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            -t "$REGISTRY/catalog:${{ github.sha }}" \
            -t "$REGISTRY/catalog:latest" \
            src/catalog

      - name: Run Trivy (image)
        uses: aquasecurity/trivy-action@cf5c088a69634cd13ccddc735d7926162c31f9a6 # master
        with:
          image-ref: ${{ steps.login-ecr.outputs.registry }}/catalog:${{ github.sha }}
          severity: CRITICAL,HIGH
          exit-code: '1'
          ignore-unfixed: true

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@45f27363d4ff7d6b16baa0516490f10c52bdd5fc # master
        with:
          projectBaseDir: src/catalog
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Push image to ECR
        id: push
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          docker push "$REGISTRY/catalog:${{ github.sha }}"
          docker push "$REGISTRY/catalog:latest"
          echo "image_uri=$REGISTRY/catalog:${{ github.sha }}" >> $GITHUB_OUTPUT

  # Despliegue a dev y test en paralelo (automático)
  deploy-dev:
    name: Deploy → dev
    needs: build-and-push
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ff717079ee2060e4bcee96c4779b553acc87447c # v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64 # v2

      - name: Promote image to dev
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          SOURCE: ${{ needs.build-and-push.outputs.image_uri }}
        run: |
          docker pull "$SOURCE"
          docker tag "$SOURCE" "$REGISTRY/catalog:dev"
          docker push "$REGISTRY/catalog:dev"

      - name: Update ECS service (dev)
        run: |
          aws ecs update-service \
            --cluster retailstore-dev \
            --service catalog \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

  deploy-test:
    name: Deploy → test
    needs: build-and-push
    runs-on: ubuntu-latest
    environment: test

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ff717079ee2060e4bcee96c4779b553acc87447c # v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64 # v2

      - name: Promote image to test
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          SOURCE: ${{ needs.build-and-push.outputs.image_uri }}
        run: |
          docker pull "$SOURCE"
          docker tag "$SOURCE" "$REGISTRY/catalog:test"
          docker push "$REGISTRY/catalog:test"

      - name: Update ECS service (test)
        run: |
          aws ecs update-service \
            --cluster retailstore-test \
            --service catalog \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

  # Despliegue a prod (requiere aprobación manual en GitHub Environments)
  deploy-prod:
    name: Deploy → prod
    needs: [deploy-dev, deploy-test]
    runs-on: ubuntu-latest
    environment: prod

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ff717079ee2060e4bcee96c4779b553acc87447c # v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64 # v2

      - name: Promote image to prod
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          SOURCE: ${{ needs.build-and-push.outputs.image_uri }}
        run: |
          docker pull "$SOURCE"
          docker tag "$SOURCE" "$REGISTRY/catalog:prod"
          docker push "$REGISTRY/catalog:prod"

      - name: Update ECS service (prod)
        run: |
          aws ecs update-service \
            --cluster retailstore-prod \
            --service catalog \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}
